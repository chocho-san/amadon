name: amadon
description: A new Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: '>=0.16.0'
  freezed_annotation:
  hooks_riverpod:
  http:


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed: '>=0.13.0'
  pedantic_mono: any
  json_serializable:

flutter:
  uses-material-design: true
